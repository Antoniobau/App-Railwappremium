export default function Footer(){
  return (
    <footer className="container" style={{marginTop:40}}>
      <div style={{opacity:0.7}}>© {new Date().getFullYear()} WebSite100 — Built with ❤️</div>
    </footer>
  )
}
